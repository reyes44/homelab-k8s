apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml

configMapGenerator:
  - name: demo-web-content
    files:
      - web/index.html
      - web/style.css

generatorOptions:
  disableNameSuffixHash: false
